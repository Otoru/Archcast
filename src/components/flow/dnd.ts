/**
 * MIME type usado no drag-and-drop de blocos da sidebar para o canvas.
 * Compartilhado entre a paleta (origem) e o flow canvas (destino) para
 * evitar drift entre os dois lados.
 */
export const BLOCK_DND_MIME = "application/x-wf-block";
